# .env file (copy to .env and fill)

# A private key funded with native tokens on Base Sepolia (gas + deck fees).
PRIVATE_KEY=""

# Seed phrase used to derive the named player wallets (alice path 0/0, bob path 0/1).
# Send a little testnet ETH from the main wallet to alice/bob when needed.
SEED_PHRASE=""

# Base Sepolia RPC URL. Defaults to public nodes when empty (rate-limited).
BASE_SEPOLIA_RPC_URL=""
